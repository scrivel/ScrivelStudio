# global beforeEach, describe, it, assert, expect
"use strict"

describe 'ElementCollection Collection', ->
  beforeEach ->
    @ElementCollectionCollection = new Editor.Collections.ElementCollection()
